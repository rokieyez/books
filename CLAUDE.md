# 서가 뒤의 방 (post-libros)

개인 서재 사이트. 1,300여 권의 실물 장서를 디지털 서가로 옮기고, 수집한 문서·사진·링크를 아카이브로 보관한다. "책장인 줄 알았는데 문이었다" — 벽마다 숨은 방이 있는 밤의 서재 콘셉트.

## 코드만 봐서는 알 수 없는 것

- **디자인은 단일 다크 세계다.** 낮/밤 테마 전환이 없는 것은 누락이 아니라 의도 — 이 서재에는 램프와 촛불만 있다. 라이트 테마를 추가하지 말 것.
- **시안 확정본**: https://claude.ai/code/artifact/89f286da-1677-4b8f-82f0-b891ea3a0a74 (시안 6 최종). 다른 시안 6종은 탈락안이므로 참고만.
- **공개 서재다 (2026-09-01 전환, 주인의 지시).** 프라이빗함을 추구하지 않는다 — 보통의 개인 홈페이지처럼 **방문자도 실제 장서·요약·기록·이음을 전부 읽는다.** 주인만의 것은 쓰기(꽂기·고치기·빼기)와 작업대(궤짝·들이기·백업)뿐. 예전의 방문자용 표본 데이터는 지웠고, `js/data.js` 에는 벽의 뼈대만 남았다. 장서는 로그인과 무관하게 `loadRealLibrary` 가 싣는다 — 다시 표본을 만들지 말 것.
- **"기록의 벽"(아카이브)은 검색에서 의도적으로 제외**되어 있다 — 숨은 방이라는 세계관 때문. 실서비스에서 아카이브 검색을 넣을 때는 별도 진입로를 쓸 것.
- 걸쇠 책(비뚤어진 책)의 위치는 벽마다 난수로 정해지지만 시드가 고정이라 항상 같은 자리다.
- **벽에는 66권만 그린다 — 이건 못 고치는 게 아니라 그림이다.** 벽이 벽처럼 보이려면 책등 수가 일정해야 한다. 그래서 장서 전체를 훑는 일은 목록·표지 뷰가 맡고, 벽은 검색할 때만 응답한 책을 앞줄로 끌어온다. 벽에 1,300권을 다 그리려 하지 말 것.
- **상단 셈에 목표치(1,300 따위)를 박지 말 것** — 서재는 계속 자라고 일부만 옮겨 둘 수도 있다 (주인의 지시). 막대는 목표 대비가 아니라 읽음 비율이다.
- **책등은 실물을 따른다** (`js/auth.js` 의 `shapeForShelf`): 키는 실제 높이 mm × 0.52 (70~130px), 두께는 등두께 mm × 1.1 (12~36px) → 없으면 쪽수 어림(13~34px) → 그것도 없으면 제목 해시. 결정적이어야 한다 — 새로고침마다 서가가 달라 보이면 안 된다.
- **목록의 시리즈 접기**: 제목 끝 「… 숫자」(1~3자리, 구분자 필수)를 밑동으로 접고, 같은 밑동·지은이 2권 이상이면 시리즈다. 정렬·검색 중에는 접지 않는다. 「1984」처럼 제목 자체가 수인 책은 4자리라 안 접힌다.
- **자동 백업**: pg_cron `daily-library-snapshot` 이 매일 04:17 KST 에 `backup_snapshots` 로 장서 전체를 jsonb 스냅샷 (30일 보관, 주인만 조회). 복원은 SQL 로 payload 를 풀면 된다.
- **갈피**: `books.bookmark_page` — 읽는 중일 때만 서표에 보이고, 오늘의 책이 읽는 중 책을 우선해 갈피를 알려준다.
- **읽은 해**: `books.read_year` — 서표에서 읽음으로 바꾸는 순간의 해가 저절로 적힌다. 이미 적힌 해는 덮지 않는다 (옛날에 읽은 책을 나중에 등재할 수 있다). 읽음일 때만 서표에 보이고 손으로 고칠 수 있다.
- **책 사이 이음**: `book_links` — 방향 없는 연결(A↔B 한 줄). 뒤집힌 중복(B→A)은 least/greatest 유일 색인이 막고, 클라이언트는 `or(book_id.eq, linked_book_id.eq)` 로 양쪽을 다 찾는다. 서표의 「이음」 줄에서 잇고 푼다.
- **CSV 되들이기**: 내보내기 맨 뒤의 「아이디」 열이 열쇠다 — 되들일 때 아이디가 맞는 책만 고치고, 아이디 없는 줄은 건너뛴다. **파일에 있는 열만 고친다** (옛 CSV 가 새 열을 지워 버리지 않게). 빈 칸은 null 로 적히므로, 지우고 싶지 않은 열은 파일에서 열째로 빼면 된다.
- **키 순서 토글**: 서가 뷰의 「키 순서」 단추 — 실물 높이 내림차순으로 다시 꽂아 본다. localStorage(`pl-heightsort`)에 남고, 검색의 앞줄 끌어오기 규칙이 우선한다.
- **궤짝의 책등 조각**: 후보에 `spine_box` 와 사진 경로가 있으면 원본 사진에서 그 부분만 canvas 로 오려 곁들인다 — 흐린 글자를 사람 눈으로 대조하라는 것. 사진 한 장당 서명 주소·그림은 한 번만 받는다.
- **갈피 시점**: `books.bookmark_at` — 갈피를 적는 순간이 같이 남고, 오늘의 책·책상 더미가 「3주 전 멈춤」처럼 알려준다. `updated_at` 은 아무 칸이나 고쳐도 움직여서 이 용도로 못 쓴다.
- **책상 더미**: 읽는 중인 책이 책상 위에 눕혀 쌓인다 (다섯 권까지, 길이는 실물 키). 누르면 서표.
- **그 해의 서재**: 통계의 회고 패널 — `read_year` 가 있는 책으로 가장 최근 해의 권수·쪽수·분류 상위를 센다.
- **이음의 별자리는 컬렉션이다 (주인의 뜻, 2026-09-01)**: 다른 작가·다른 작품이라도 이었을 때 빛이 나는 짝을 손으로 이어 둔 것. 그래서 시리즈 잇기 제안은 여기에 두지 않는다 — **서표의 이음 줄 밑 「~권을 시리즈로 잇는다」 단추**(주인만, 같은 무리 2권 이상일 때)가 이웃 권끼리 사슬처럼(1↔2, 2↔3…) 잇는다. 이미 있는 이음은 건너뛴다. **같은 시리즈끼리의 이음은 별자리에 그리지 않는다** — 이음 자체는 남아 서표의 이음 줄에는 보이되, 성좌에서는 걸러진다(자동 밑동·손묶기 둘 다). 다른 작품과의 이음은 시리즈 책이라도 그대로 뜬다.
- **기록 일괄 짓기**: 들이기 화면의 단추 — 읽음 책 중 기록 없는 것만 서른 권까지 순서대로 짓는다 (권마다 AI 비용, 멈춤 가능). 이미 있는 기록은 summarize-book 이 재사용하므로 이중 과금은 없다.
- **함수 오류는 tellWhy 가 살린다** (`js/db.js`): Edge Function 이 4xx/5xx 로 답하면 supabase-js 는 "non-2xx" 라고만 말한다 — 몸통의 `{error:…}` 를 꺼내 error.message 에 되살린다. 함수 호출을 새로 만들면 `.then(tellWhy)` 를 꼭 붙일 것.
- **서표의 지우개들**: 「표지를 지운다」·표지 주소 직접 입력, 「서지를 지운다」(ISBN·쪽수·크기·표지·펴낸곳·펴낸해 비움 + 표식 걷음 — 제목·지은이·메모는 남김). 엉뚱한 판본 서지가 입혀졌을 때의 출구다.
- **작가의 별자리**: 통계 canvas — 두 권 넘게 모은 작가 여덟 명이 각각 성좌(가운데 이름, 둘레 책들). 이음과 달리 손대지 않아도 뜬다. 읽은 책 별이 더 밝다. **성도(星圖)처럼 그린다**: 별 자리는 작가 이름 시드 난수로 흩뿌리고(새로고침해도 같은 하늘) 바퀴살 대신 이웃 별끼리 사슬로 잇는다. 잔별 배경 + rAF 로 반짝임·미세 표류 — 모션 감경이면 정지 화면 한 장, 화면에서 사라지면(isConnected) 루프가 멈춘다. 별 클릭 판정은 표류를 반영한 현재 좌표로 한다.
- **시리즈 손묶기**: `books.series`(text) — 서표의 「시리즈」 칸으로 제목 꼴이 제각각인 전집(「신들의 사회」 「빛의 왕」…)도 한 이름으로 묶는다. 목록 접기에서 **손으로 적은 시리즈가 제목 끝 숫자 규칙(자동)보다 우선**한다 (`seriesRows` 의 「손␟」 열쇠). 칸에는 datalist 로 이미 쓰던 시리즈 이름을 보여줘 오타로 무리가 갈라지는 것을 막는다. CSV 내보내기·되들이기에 「시리즈」 열이 있다.
- **표지는 큰 그림(cover500)으로 승급한다**: 알라딘 API 의 기본 표지는 엄지손톱(coversum)이라 흐리다. enrich-books 의 `bigCover` 가 `/cover500/` 주소로 올려 보고 **HEAD 로 실재를 확인한 뒤에만** 채택한다 (옛 책은 큰 그림이 없을 수 있다 — 그때는 원래 주소). 기존 196권도 같은 방법으로 전수 검증 후 DB 를 교체했다 (2026-09-01).
- **ISBN 조회의 사다리** (enrich-books `lookupHard`): ItemLookUp 은 절판·옛 책에서 곧잘 빈손이 된다(사이트에는 보이는 책도). 실패하면 그 번호로 ItemSearch(Book→Foreign→All)를 돌려 **ISBN 이 정확히 일치하는 항목만** 받아 ItemId 로 재조회한다. 사람이 번호를 붙여넣는 서표·바코드 길에서만 쓴다 — 자동 채우기는 이미 검색을 거친 뒤라 불필요.
- **실물 책등**: 인식이 책마다 자리 상자(`spine_box`, 0~1000 비율)를 주면, 클라이언트(`intake.js` 의 `cropSpines`)가 책장 사진에서 그 부분만 webp 로 오려 covers 버킷 `<uid>/spines/<책id>.webp` 에 저장하고 `spine_url` 에 경로를 적는다. 서가는 조각이 있으면 그것을 입고 글자를 숨긴다(그림 안에 이미 있다). 이미 꽂힌 책은 사진의 「다시 읽는다」가 자리를 소급해 채운다 — 중복은 안 생기고 상자만 붙는다. **상자의 정확도는 모델에 달렸다** — 옆 책이 섞여 보이면 여백(pad)을 줄이거나 프롬프트를 다듬을 것.

## Supabase

- 프로젝트 `post-libros`, ref `gaeumegwhxxnfvrhbknp`, 서울(ap-northeast-2), 조직 `rokieyez`
- **무료 플랜 슬롯을 다 썼다** (수아요나 프로젝트와 합쳐 2/2). 세 번째 프로젝트가 필요하면 유료 전환이나 기존 프로젝트 일시정지가 먼저다
- 무료 플랜은 오래 쓰지 않으면 프로젝트가 일시정지된다 — 개발을 쉬었다 돌아오면 대시보드에서 깨워야 할 수 있다
- **읽기는 공개, 쓰기는 주인만.** books·book_summaries·archive_items·book_links 의 select 는 anon 도 통과한다 (공개 서재 전환). insert/update/delete 와 intake_photos·intake_candidates·backup_snapshots 은 여전히 주인만. covers 버킷은 공개(public URL, 서명 불필요), intake 버킷은 비공개
- **표지(cover_url)는 알라딘 CDN 주소를 그대로 쓴다** — 우리 스토리지에 없다. 죽은 주소는 화면의 error 핸들러가 걷는다
- **한 사람만 쓰는 서재다.** `shouldCreateUser: false` 로 화면에서 막아 두었지만 진짜 잠금은 대시보드의 신규 가입 차단이다 (API 를 직접 부르면 화면 쪽 설정은 무력하다)
- **메일 링크 로그인을 지우지 말 것.** 평소에는 비밀번호로 들어오지만, 가입이 잠겨 있어 비밀번호를 잊으면 계정을 다시 만들 길이 없다. 메일 링크가 유일한 예비 문이고, 비밀번호는 들어와 있는 동안에만 정할 수 있어 처음 한 번도 이 문을 쓴다
- 스토리지 파일은 반드시 `<uid>/파일명` 으로 올린다 — 정책이 첫 폴더를 주인의 uid 로 확인한다
- **Edge Function 에서 service_role 키를 쓰지 말 것.** 부른 사람의 토큰(Authorization 헤더)을 그대로 넘겨 클라이언트를 만들면 RLS 가 그대로 걸린다. service_role 을 쓰는 순간 그 함수는 모든 잠금을 지나간다
- **인증 호출에는 반드시 시간제한을 둔다** (`js/db.js` 의 `guard`). supabase-js 는 탭 사이에 자물쇠를 걸고 기본값이 무한 대기라, 다른 탭이 쥔 채 멈추면 로그아웃조차 돌아오지 않는다. 탭 자물쇠(`tabLock`)에도 5초 제한을 두었다
- 스키마 변경은 Supabase 에 적용한 뒤 `supabase/migrations/` 에 같은 이름으로 남긴다
- **`.limit(2000)` 은 2,000줄을 보장하지 않는다.** PostgREST 에 한 번에 돌려주는 줄 수의 상한(기본 1,000)이 따로 있고, 넘치면 오류 없이 조용히 잘린다. 장서가 1,300권이라 실제로 손해가 난다 — `listBooks` 는 `.range()` 로 쪽을 나눠 끝까지 읽는다. 정렬에 `id` 를 마지막으로 거는 것은 벽·단·자리가 겹칠 때 쪽이 어긋나지 않게 하려는 것이다

## 배포

- 저장소는 `rokieyez/books` — **경로가 저장소 이름을 따라가므로 이름을 바꾸면 주소가 깨진다** (로컬 폴더명 post-libros 와 다른 것은 의도)
- 주소: https://www.rokiz.net/books/ — 도메인은 루트 사이트 저장소(`rokieyez/rokieyez.github.io`)의 CNAME 파일이 정하고, 이 프로젝트는 그걸 상속만 한다
- 도메인 등록처는 닷네임. DNS: `www` CNAME → rokieyez.github.io, 루트 → GitHub A 레코드 4개
- 배포는 main 에 `git push` 하면 끝 (GitHub Pages legacy 빌드)
- Supabase Redirect URLs 에 `https://www.rokiz.net/**` 가 있어야 메일 링크 로그인이 산다

## 중복은 DB 가 막는다

같은 책이 두 번 꽂히지 않게 하는 규칙은 **세 곳에 같은 모양으로** 있다. 하나만 고치면 화면은 통과시키고 DB 가 거절하는 엇갈림이 생긴다.

1. `books.dedup_key` (생성 열) + 유일 색인 — 최후의 방어선
2. Edge Function `recognize-spines` 의 `keyOf()` — 넣기 전에 걸러 "이미 있음 N권"을 알려주려고
3. ISBN 이 있으면 `(owner_id, isbn)` 유일 색인이 먼저 잡는다

열쇠 만드는 법: 제목과 지은이를 이어 붙이고 소문자로 바꾼 뒤 숫자·영문·한글이 아닌 것을 모두 지운다. 숫자는 남기므로 "삼대 26"과 "삼대 27"은 다른 책이다.

## 벽·단은 스스로 정해진다

분류가 곧 벽이다 (역사→역사, 과학→과학, 예술·사회→예술사회, **종교→종교**, 나머지→문학). 이 규칙도 세 곳에 있다: DB 의 `wall_for_category()`, Edge Function 의 `wallFor()`, `js/intake.js` 의 `WALL_OF`. **종교의 벽은 주인의 요청으로 생긴 다섯째 벽**(2026-09-01) — `js/data.js` 의 벽 뼈대에 들어 있다. recognize-spines 의 분류 enum 에도 종교가 있다.

**화면의 벽 배치는 wall 열이 우선**이고, 없을 때만 분류로 정한다 (`auth.js` 의 byWall). 사진에 적은 벽·서표에서 옮긴 자리가 화면에 그대로 보여야 하기 때문 — 분류로만 묶으면 자리를 옮겨도 벽이 안 바뀐다.

사진에 벽·단을 적어 두었으면 그것이 우선이다 — 실제로 찍은 자리가 더 정확하니까. 비워 두면 분류로 정하고 한 단에 서른 권씩 채운다. 어디까지나 1차 배정이고, 서표에서 한 권씩 고칠 수 있다.

**분류 이름과 벽 이름은 다르다.** 예술·사회·인문·철학이 한 벽(예술사회)을 쓴다. 표에 없는 분류는 fallback 이 조용히 문학 벽으로 보내므로(실제로 「인문」 한 권이 그렇게 섞여 있었다, 2026-09-01) 새 분류가 생기면 `auth.js` 와 `intake.js` 의 `WALL_OF` 두 표에 모두 넣을 것.

**문학 벽은 갈래로 단을 가른다** (`books.genre`). 장서의 열에 아홉이 문학 벽에 몰려 벽이 아니라 창고가 되기 때문이다. 알라딘 분류명을 한 겹 더 읽어(`genreOf`) 소설·고전·에세이·시와 희곡·추리·환상과 SF 로 나누고, 널빤지에 이름표를 붙인다 (`shelfLines` in app.js). **서재의 40%를 넘게 가진 벽에만, 갈래를 아는 책이 10권 넘을 때만** 갈라진다 — 그 외에는 예전처럼 스물둘씩 세 줄. 문학이 아닌 책에는 갈래가 없으므로 `"―"` 를 적어 다시 묻지 않게 한다 (화면에서는 null 로 취급).

## 읽은 책만 걷는 길

서재의 95%가 아직 안 읽은 책이라, 그냥 둘러보는 사람은 좀처럼 「읽어 낸 책」을 만나지 못한다. 그래서 검색과 **같은 층위의 거름망**을 하나 두었다 (`stFilter` in app.js): 위쪽 인구조사 막대(넓은 화면)나 현관의 「읽은 책만」 단추(좁은 화면 — 막대는 560px 아래에서 숨는다)를 누르면 켜진다.

거름망은 `passes(b)` 하나로 모든 보기에 걸린다 — 벽의 응답 수, 책등 흐림, 표지·목록·별자리까지. **검색만 확인하는 `matchBook` 을 직접 부르지 말고 `passes`/`sifting()` 을 쓸 것.**

## 지금 펼친다

책상 위 더미·갈피·읽기 리듬은 전부 「읽는 중」을 먹고 사는 기능인데, 실제로 읽는 중인 책이 0권이라 넷 다 잠들어 있었다. 서표의 「지금 펼친다」 단추 하나가 상태를 읽는 중으로 바꾸고 갈피를 1쪽에 꽂아 그 기능들을 한꺼번에 깨운다. 안 읽음일 때만 보인다 (`syncOpenNowRow`).

## 이음은 길이다 — 항로도와 현관의 길

별자리는 「어느 책이 어느 책과 짝인가」만 보여줬다. 커뮤니티에 도는 독서 플로우 차트처럼 **어디서 시작해 어디로 가는가**를 보여주려고 방향과 이름을 붙였다.

- `book_links.kind` = `순서`(기본) | `나란히`. 순서만 화살표가 되고 길이 된다. 방향은 `book_id → linked_book_id` 로 이미 있었다 — 서표의 이음 줄 「다음에/먼저/나란히」 단추가 `flipLink`(양끝 맞바꾸기)와 `updateLink({kind})` 로 세 상태를 돈다
- `books.path_name` = 이 책**에서 시작하는** 길의 이름. 입구 책에만 적는다 — 길 중간 책에 적어도 화면에는 안 나온다
- **항로도의 자리 잡기는 덩어리 채우기다** (`renderLinkWeb`): 길 하나(이어진 덩어리)를 깊이순으로 가로로 펴고, 덩어리끼리는 한 줄에 들어가는 만큼 채운 뒤 줄을 바꾼다. 남은 칸에 들어가는 덩어리를 뒤에서 끌어오는 first-fit 이라 두 칸짜리 길 열한 개가 세로로만 쌓이지 않는다 (그렇게 하면 3,500px 두루마리가 됐다)
- **현관의 「걸어 볼 만한 길」**(`renderPaths`)은 항로도와 따로 센다 — 통계 안쪽까지 들어가는 사람은 드물기 때문. 캔버스도 표지도 안 쓰고 입구에서 앞으로만 따라가는 가벼운 계산이다. 이름 붙인 길이 먼저, 그다음 긴 길, 넷까지
- **길을 걷는다**: `walk = { list }` 를 두면 서표에 「이 길의 다음 책」 줄이 뜬다. 서표를 닫으면 walk 도 풀린다

## 주소에 남는 것

- `#book/<id>` — 서표 한 권
- `#covers` · `#list` · `#stats` — 보기 (서가는 해시 없음)

**`replaceState` 는 hashchange 를 울리지 않는다** — 그래서 `hashSelf` 표는 해시를 실제로 바꿀 때만 남긴다. replaceState 앞에 표를 남기면 그다음에 오는 진짜 뒤로 가기를 삼켜 버린다.

## 책 한 권의 주소

`#book/<id>` — 서표를 열면 해시가 바뀌고, 그 주소로 들어오면 서표가 열린 채 서재가 시작된다 (`openFromHash`, 장서를 다 실은 뒤 `auth.js` 가 한 번 부른다). **내가 쓴 해시와 사람이 누른 뒤로 가기를 구별해야 하므로 `hashSelf` 표를 쓴다** — 이 표가 없으면 서표를 열 때마다 hashchange 가 되울려 무한히 다시 연다. 서표를 닫을 때는 `replaceState` 로 지워 뒤로 가기 기록을 더럽히지 않는다.

## 못 연 것과 비어 있는 것은 다르다

**화면이 거짓말을 하지 않게 하는 규칙이다** (2026-09-02, 실패 경로를 실제로 막아 보고 고침).

- 장서 싣기가 실패하면 `PostLibrosShowEmpty(err)` 에 **까닭을 함께 넘긴다.** 예전에는 인자 없이 불러 `renderAll()` 이 돌았고, 그러면 벽 다섯이 전부 「이 벽은 아직 비어 있습니다」, 셈은 「아직 비어 있음」, 오늘의 책은 「사진을 들이는 것부터 시작합니다」라고 말했다 — 통신 장애를 **텅 빈 새 서재**로 보여준 것이다. 지금은 「서재를 열지 못했습니다」와 「다시 열어 본다」가 뜬다
- **「여는 중」에는 끝이 있어야 한다.** 싣기가 아예 시작되지 못하면 아무도 ShowEmpty 를 부르지 않는다 — jsdelivr 가 죽어 supabase-js 가 안 오면 `db.js` 가 `PostLibrosDB` 를 못 만들고, auth.js 는 `if (!db) return` 으로 서지도 않는다. 실제로 24초 뒤에도 「서재를 여는 중…」이었다. app.js 에 **열두 셈짜리 시간제한**을 두어 어떤 까닭이든 말이 나오게 했다
- 그때는 `PostLibrosRefresh` 조차 없으므로 「다시 열어 본다」가 **문서를 통째로 다시 연다**
- 참고로 **폰트 서버(fonts.googleapis.com)가 죽는 것은 문제가 없다** — `display=swap` 이라 오히려 더 빨리 뜬다. 막아 보고 확인했다

## 작업대는 주인이 들어온 뒤에 온다

`js/intake.js` 는 51KB 인데 오로지 주인의 것이다(사진 들이기·궤짝·백업·채우기). 예전에는 `index.html` 이 모두에게 내려보냈다 — 첫 화면 짐 368KB 중 칠분의 일을, 방문자는 결코 열 수 없는 화면에 쓰고 있었다. 지금은 `auth.js` 의 `loadWorkbench()` 가 **주인이 확인된 뒤 한 번만** `<script>` 를 꽂는다 (방문자 짐 368KB → 322KB).

intake.js 는 `readyState` 를 보고 스스로 mount 하므로 늦게 와도 붙는다 (확인함). `window.PostLibrosWallOf` 도 여기서 나오는데, 그걸 쓰는 곳은 궤짝(주인 전용)뿐이고 `?.` 로 받는다.

## 손가락이 닿는 크기

**이 서재를 처음 보는 사람은 대개 전화기로 들어온다.** 390px 아이프레임에 실어 실제로 재어 보니 사다리 눈금이 7×7px, 서표의 닫기가 23×21px 이었다 — 마우스로는 되지만 손가락으로는 안 되는 크기다 (WCAG 2.2 의 최소는 24×24). 보이는 크기는 그대로 두고 **`::after` 로 닿는 자리만 넓혔다** (눈금은 34×30). 새 단추를 만들 때는 같은 자로 재 볼 것 — 눈으로는 알 수 없다.

**「보기 전환」 줄은 `left:0; right:0`** 로 편다. 예전의 `left:50% + translateX(-50%)` 는 자리 잡힌 요소의 폭을 「현관 폭의 절반」으로 가둬, 단추가 여덟 개가 되자 632px 에서 잘려 탭 글자가 「서 / 가」로 쪼개졌다. `viewIn` 키프레임에도 같은 translateX 가 있었고, 그것만 남겨 두면 폰에서 줄이 통째로 화면 왼쪽 밖으로 밀려난다.

## 「다시 읽는다」는 안전하다 (2026-09-02 확인)

사진의 「↻ 다시 읽는다」는 **같은 책을 두 번 꽂지 않는다** — `recognize-spines` 가 이미 꽂힌 책을 열쇠로 걸러 내고, 없던 `spine_box` 를 소급해 채운다. v10 부터는 **같은 사진의 「대기」 후보도 지우고 새로 담아** 궤짝이 불어나지 않는다 (확정·버림은 사람의 판단이므로 건드리지 않는다). 남는 값은 AI 호출 비용뿐이다. 화면의 경고 문구도 「같은 책이 또 꽂힙니다」(거짓)에서 「비용이 듭니다」(사실)로 고쳤다.

**그런데 자리 상자가 전 장서에 0개다** (2026-09-02 확인: `spine_box` 있는 책 0권, 후보 0건). 18장의 사진이 상자 기능이 생기기 전 코드로 읽혔기 때문이다. **실물 책등 조각을 살리려면 사진을 다시 읽는 수밖에 없다** — 들이기의 「사진에서 책등을 오린다」는 상자가 없으면 그 사실과 다음 할 일을 말해 준다.

## 방문자가 만나는 순서

이 서재의 약점은 그림이 아니라 **얇은 데이터**다 (520권 중 서지 38%, 기록 6권, 메모 0). 그래서 채우는 일에는 순서를 둔다.

- **기록 일괄 짓기**는 읽음 → 읽는 중 → 표지 있는 책 순이다. 표지도 없고 읽지도 않은 책은 아무도 열지 않으므로 아예 줄에 세우지 않는다
- **실물 책등**은 사진 인식이 남긴 `spine_box` 가 있는데도 오려 붙이는 일이 한 번도 돌지 않아 조각이 0개였다. 들이기의 「사진에서 책등을 오린다」가 밀린 것을 한꺼번에 처리한다 — 사진과 상자만 쓰므로 **알라딘도 AI 도 부르지 않는다. 비용이 없으니 먼저 돌릴 것**
- **판형 지도**(통계)는 `size_height`/`size_depth`(mm)를 숫자가 아니라 크기로 보여준다. 판형 이름은 높이로 가른다(문고본 ~155, 국판 ~195, 신국판 ~213, 크라운판 ~232, 4×6배판 ~265). 치수를 아는 책이 8권 미만이면 패널째 숨는다

## 알라딘

공식 매뉴얼이 구글 문서로만 있어 읽히지 않는다. **아래는 실제 응답을 찍어 확인한 것**이다 (`enrich-books` 의 살펴보기 모드: `{"probe":"검색어"}`).

```
https://www.aladin.co.kr/ttb/api/ItemSearch.aspx
  ?ttbkey=…&Query=…&QueryType=Keyword&MaxResults=5&start=1
  &SearchTarget=Book&output=js&Version=20131101
```

`item[]` 안에 `title` `author`("박상륭 (지은이)" 꼴) `pubDate`("2020-07-01") `isbn` `isbn13` `cover` `publisher` `categoryName`("국내도서>소설/시/희곡>…") 이 온다. **제목·설명에 HTML 실체(`&lt;` `&amp;`)가 섞여 오므로 반드시 풀어서 쓸 것.**

- **분류 규칙(`catOf`)은 겉보기보다 위험하다.** 「고전」을 역사로 보내던 규칙 때문에 나니아 연대기 일곱 권·오디세이아·소포클레스 비극 전집·봄봄·메밀꽃 필 무렵이 전부 역사로 분류돼 **역사의 벽이 0권**이 되어 있었다 (2026-09-01 발견, 규칙에서 「고전」을 뺐고 22권을 문학으로 되돌렸다). 규칙을 손볼 때는 **실제 `categoryName` 을 먼저 찍어 볼 것** — `{"lookup":"<isbn13>"}` 살펴보기 모드가 분류명을 함께 돌려준다
- **제목은 자동으로 고치지 않는다.** 「죽음의 한 연구 상」을 검색하면 단권본이 잡힌다. 고치면 상·하가 같은 제목이 되어 중복 색인에 부딪힌다. 알리기만 하고 판단은 사람에게 남긴다
- 지은이는 고친다 — 잃을 정보가 없다
- **하루 5,000건 제한 — 넘기면 키가 통째로 차단된다** (실제로 겪음, 2026-09-01): 모든 호출이 `errorCode 4, "API출력이 금지된 회원입니다"` 로 거부되고, 조회·검색이 전부 빈손이 되어 멀쩡한 ISBN 도 「없는 번호」처럼 보인다. 보통 자정(한국 시간) 지나면 풀린다. 함수의 카나리아(`aladinDown`)가 이 상태를 알아채 429 로 진짜 이유를 알려준다. **대량 회차 반복(채우기·궤짝)을 하루에 몰아 돌리지 말 것** — 권당 2~4회 호출이라 1,300권 서재에서는 한 번의 전수 회차로도 한도의 절반을 쓴다
- 20권씩 나눠 돌고, 화면의 단추 한 번으로 남은 것이 없을 때까지 스스로 돈다
- **쪽수·실물 크기는 검색(ItemSearch)에 없다.** ISBN 을 알아낸 뒤 조회(ItemLookUp, `OptResult=packing`)를 한 번 더 불러 `subInfo.itemPage` 와 `subInfo.packing.sizeHeight/sizeDepth`(mm) 를 읽는다 — 그래서 책 한 권에 호출이 둘이다. **itemPage 는 실제 데이터로 확인됨** (2026-09-01, 별의 계승자 344쪽 등 3권). **packing 도 실측 확인됨** (같은 날, 199권이 실물 크기로 채워짐). 응답 모양이 궁금하면 `{"lookup":"<isbn13>"}` 살펴보기 모드로
- **바코드 입고**: `{"add_isbn":"<isbn>"}` 이 조회 한 번으로 서지가 완성된 책을 꽂는다. 벽은 DB 의 `wall_for_category()` 를 rpc 로 불러 정한다 — 규칙 사본을 늘리지 않기 위해. 화면은 모바일 크롬의 `BarcodeDetector`(EAN-13)로 읽고, 미지원 브라우저에는 손입력 칸만 남는다
- **검색은 사다리다** (실사용 못찾음 20권 분석에서 나온 구조): ① 국내도서 키워드 → ② 권수 뗀 제목만(Title 검색) → ③ 외서(SearchTarget=Foreign) → ④ 웹사이트 검색. ④는 HTML 에서 `wproduct.aspx?ItemId=` 만 줍고 **서지는 반드시 조회 API 로** 받는다 — 화면 구조가 바뀌어도 깨지는 건 발견뿐, 데이터는 더러워지지 않는다. 앞머리 광고 링크도 ItemId 를 갖고 있으므로 점수로 걸러야 한다
- **제목 비교는 `matchScore`** (편집거리 `similar` 가 아니라): 부제 긴 판본(「백야행 1 - 하얀 어둠 속을 걷다」)은 포함 관계로 후하게, 권수가 어긋나면(1권↔2권) 크게 깎는다. 다섯 결과 중 최고점을 고른다 — **첫 결과만 보면 「채식주의자」도 못 찾는다** (실제로 그랬다)
- **궤짝 확정**: `{"crate":true,"limit":20}` 이 확신 낮은(0.6 미만) 궤짝 후보를 알라딘에 물어, `matchScore` 0.75 이상이면 **알라딘 서지를 통째로(제목까지) 채택해** 꽂는다. 「제목은 자동으로 고치지 않는다」의 예외다 — 궤짝 후보의 제목은 사람이 확인한 적 없는 흐린 OCR 이라, 덮어써서 잃을 것이 없다. 못 정한 후보는 대기로 남는다(잃는 것이 없다). 화면 단추는 확정+겹침이 0이 되면 스스로 멈춘다 — 남은 것은 알라딘도 모르는 책이므로 더 돌아도 같다
- **사다리의 다섯째 칸은 국립중앙도서관 ISBN 서지정보**: `https://www.nl.go.kr/seoji/SearchApi.do?cert_key=…&result_style=json&page_no=1&page_size=1&isbn=…` (공식 문서 [N31101030500](https://www.nl.go.kr/NL/contents/N31101030500.do) 에서 확인한 주소 — `seoji.nl.go.kr/landingPage/SearchApi.do` 도 응답하지만 문서에 적힌 것은 이쪽이다). 알라딘이 통째로 막힌 날의 유일한 길이다. 필드는 `TITLE` `AUTHOR` `EA_ISBN` `PUBLISHER` `PAGE`("349 p.") `BOOK_SIZE`("152*225mm" — 뒤 숫자가 높이) `PUBLISH_PREDATE` `TITLE_URL`(**표지 이미지 주소 — 있다**). 오류는 `{RESULT:"ERROR", ERR_CODE:"011"}` 꼴로 `docs` 없이 온다. 비밀값 `NLK_CERT_KEY` 가 없으면 조용히 건너뛴다 — **키를 넣은 뒤 `{"nlk":"<isbn>"}` 살펴보기 모드로 실제 응답을 한 번 찍어 확인할 것** (2026-09-01 기준 아직 미검증)
- **국중 인증키 신청**: [이용안내](https://www.nl.go.kr/NL/contents/N31101010000.do) → [인증키 신청하기](https://www.nl.go.kr/NL/contents/N31101020000.do) (로그인 필요). 절차는 ① 회원가입 ② 인증키 발급 신청 ③ **담당자 승인** ④ 사용 — 즉시 발급이 아니다. 담당 부서 디지털정보기획과 02-590-0548
- **호출 계기판은 어림이 아니라 알라딘에게 직접 묻는다.** `enrich_tried_at` 로 센 「오늘 물어본 권수 × 2.5」는 **바닥값일 뿐** 실제 사용량이 아니다 — 2026-09-01 에 어림은 1,198 인데 키는 이미 차단돼 있었다. 그래서 계기판은 열 때와 회차 끝마다 `{"probe":"책"}` 으로 살아 있는지 직접 확인하고, 막혔으면 「채운다」·「갈래를 받아온다」 단추를 잠근다
- **갈래 채우기 모드**: `{"genre":true,"limit":40}` 은 ISBN 을 아는 책만 골라 조회 한 번으로 갈래를 채운다 — 권당 1회라 서지 채우기보다 훨씬 싸다. 알라딘이 막힌 상태에서 눌러도 아무것도 쓰지 않고 그대로 멈춘다(확인함)
- **다 물어본 책에는 `books.enrich_tried_at` 표식이 남는다.** 이것이 없으면 알라딘이 모르는 책이 대기줄 맨 앞에 쌓여 자동 반복이 같은 스무 권만 영원히 묻는다. 서표에서 제목·지은이를 고치면 표식이 걷혀 다시 묻는다. 서표의 「알라딘에서 받아온다」는 `{"book_id":…}` 로 그 한 권만, 표식을 무시하고 묻는다

## 로드맵 (2026-09-01 기준)

1. **1단계 (완료)** — 시안을 정적 사이트 구조로 이식
2. **2단계 (완료)** — Supabase 스키마와 RLS: `books` / `book_summaries` / `archive_items` / `archive_book_links` / `intake_photos` / `intake_candidates`. 표와 storage 버킷(covers·intake·archive)을 함께 잠갔고 보안 린터 통과 확인
3. **3단계 (완료)** — 로그인. 평소에는 비밀번호, 예비로 메일 링크. 화면은 로그인하면 실제 장서를 따라간다
4. **4단계 (거의 완료)** — 책 등록 파이프라인
   - (완료) 책장 사진 업로드 — `js/intake.js`. 3000px 초과분만 줄인다: 나중에 책등을 잘라 확대해 읽어야 할 수 있어 원본 해상도를 함부로 버리지 않는다
   - (완료) AI 비전으로 책등 인식 — Edge Function `recognize-spines`. 확신도 0.85 이상이면 벽에 꽂고 아래면 궤짝으로. **비밀값 `ANTHROPIC_API_KEY` 가 있어야 돈다**
   - (완료) 중복 방지, 벽·단 자동 배정, 손으로 들이기, 서가에서 빼기
   - (완료) 알라딘 Open API 서지 조회 — Edge Function `enrich-books`. 아래 「알라딘」 참조
5. **5단계 (완료)** — AI 요약은 미리 만들지 않는다. Edge Function `summarize-book` — 책을 처음 열 때만 짓고 그 뒤로는 저장된 것을 읽는다. 모르는 책이면 지어내지 말라고 일러 두었다
6. **6단계 (완료)** — 배포. GitHub Pages + www.rokiz.net/books, HTTPS 강제, 파비콘·공유 이미지·404. 기록의 벽에 실제로 들이고 버릴 수 있다

### 아직 남은 것 (사용자 손이 필요)

- **국립중앙도서관 API 키** — `NLK_CERT_KEY`. seoji.nl.go.kr 에서 무료 발급. 넣기 전까지 다섯째 사다리는 잠들어 있고, 넣은 뒤 실제 응답을 한 번 찍어 확인해야 한다
- 커스텀 SMTP — 아래 참조 (급하지 않다)

### 메일 사정

보안 린터가 남긴 유일한 지적은 Leaked Password Protection 인데 **유료 옵션이라 켜지 않기로 했다.** 다시 제안하지 말 것.

메일 쪽은 확인해 보니 생각만큼 급하지 않다. Supabase 기본 SMTP 는 **조직 구성원 주소로만** 보내는데, 이 서재는 주인 한 사람만 쓰고 그 주소가 곧 조직 주인의 주소다 — 그래서 예비 문(메일 링크)은 지금도 열린다. 다만 **시간당 두 통** 제한이 있어, 몇 번 헛짚으면 그날은 더 못 받는다.

커스텀 SMTP 를 붙이면 기본 한도가 시간당 서른 통으로 올라간다. 급한 불은 아니고, 붙일 때는:

- 보낼 곳은 Resend 를 본다. 호스트 `smtp.resend.com`, 사용자 `resend`, 비밀번호는 Resend API 키. 포트는 Supabase 문서가 587 로 예시를 든다
- rokiz.net 에 **MX·TXT 가 하나도 없다** (2026-09-01 확인) — 메일용 DNS 를 새로 얹어도 웹(A·CNAME)은 건드리지 않는다
- 정확한 DNS 레코드 값은 Resend 가 도메인마다 새로 만들어 준다. 미리 적어 둘 수 없으니 대시보드가 보여 주는 것을 그대로 옮긴다

### Edge Functions 비밀값

| 이름 | 쓰는 곳 |
|---|---|
| `ANTHROPIC_API_KEY` | `recognize-spines`, `summarize-book` |
| `ALADIN_TTB_KEY` | `enrich-books` |
| `NLK_CERT_KEY` | `enrich-books` (선택 — 없으면 국립중앙도서관 사다리만 건너뛴다) |

## 규약

- 빌드 단계 없는 정적 HTML/CSS/JS (수아요나 사이트와 같은 스택 — 배포는 `git push`)
- 색·타이포는 css/style.css 상단 `:root` 토큰으로만. 차트 계열 색을 바꿀 때는 dataviz 검증기 통과 필수 (읽음 상태 3색은 검증 완료: #3E8A5F/#BC852D/#5C79D0 on #241708)
- 모든 모션은 `prefers-reduced-motion` 대응을 유지할 것
